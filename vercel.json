{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "buildCommand": "npm install --include=dev && npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
